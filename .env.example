# Copy this file to .env and fill in your values
# .env is gitignored and will NOT be pushed to the repository

# Admin password for /admin command (change this!)
ADMIN_PASSWORD=your_secret_password_here

# Optional: Port (Railway sets this automatically)
# PORT=3000

# Optional: Database URL (Railway sets this automatically)
# DATABASE_URL=postgresql://...
